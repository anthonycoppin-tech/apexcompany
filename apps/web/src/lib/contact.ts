/**
 * L'adresse affichée au public.
 *
 * **À CONFIRMER avec le client avant la mise en ligne** — elle figurait déjà en
 * dur sur `/contact` avec cette réserve, et les pages légales en ont besoin à
 * leur tour. Une adresse écrite à trois endroits en devient trois adresses le
 * jour où l'une change, et c'est justement celle qui reçoit les demandes de
 * suppression de données qui ne doit pas se perdre.
 *
 * Le RGPD veut par ailleurs une adresse **dédiée** aux demandes d'exercice des
 * droits, qui peut être celle-ci ou une autre : c'est une des questions posées
 * dans `08-CE-QUI-MANQUE.md`. Tant qu'elle n'est pas tranchée, tout arrive ici.
 */
export const EMAIL_CONTACT = 'contact@apexcompany.com';
